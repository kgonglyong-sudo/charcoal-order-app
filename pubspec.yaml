name: charcoal_order_app
description: 숯 주문 관리 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # 상태 관리
  provider: ^6.1.1
  
  # 라우팅
  go_router: ^12.1.1
  
  # Firebase (나중에 사용)
  #firebase_core: ^2.24.0
  #firebase_auth: ^4.15.0
  #cloud_firestore: ^4.13.0
  #firebase_storage: ^11.6.0
  
  # UI 라이브러리
  cupertino_icons: ^1.0.2
  
  # 유틸리티
  intl: ^0.19.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/logo-bg.png